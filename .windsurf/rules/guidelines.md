# Windsurf Guidelines for Learn Python

- Write modular, single-responsibility functions.
- Respect immutability where appropriate (using `frozen=True` dataclasses or tuples).
- For concurrent tasks, guarantee graceful shutdowns and resource cleanups via async context managers (`asynccontextmanager`).
