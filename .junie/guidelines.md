# Junie Guidelines for Learn Python

- Ensure compatibility with Python 3.11 through 3.13.
- Favor standard library solutions when available (`dataclasses`, `functools`, `itertools`, `contextlib`).
- When writing tests, use parameterization (`@pytest.mark.parametrize`) to test edge cases comprehensively.
